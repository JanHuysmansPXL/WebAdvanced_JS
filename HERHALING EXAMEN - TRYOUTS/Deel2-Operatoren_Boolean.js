// BOOLEAN

let waarde = true;

console.log ('het gegevenstype is: ' + typeof waarde);

// TERNARY OPERATOR // VOORWAARDELIJKE OPERATOR - IF THEN ELSE?

let dag = 'Maandag';
let uitspraak;
uitspraak = (dag === 'Zaterdag') ? 'Joepie, feest!' : 'Geen feest';
console.log(uitspraak);

